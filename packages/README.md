# Packages

Planned package boundaries:

- `core`: deterministic game state, command dispatch, events, RNG and replay.
- `effects`: effect DSL operations and target resolution.
- `schema`: JSON schemas and validators.
- `simulator`: bots and deterministic sample runs.
- `app`: browser play/editor surface.
